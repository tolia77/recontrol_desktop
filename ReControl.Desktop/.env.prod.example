API_BASE_URL=https://port3003.kokhan.me
WS_URL=wss://port3003.kokhan.me/cable
ENVIRONMENT=prod
# Minimum log level recorded (Logs tab + file): debug | info | warn | error.
# warn keeps the hot path lean — the per-event Info logging during streaming
# (ICE candidates, command dispatch) is a synchronous file append per call.
LOG_LEVEL=warn
