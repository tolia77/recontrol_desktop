{
  // Debug-build overrides. Copied to the output directory only when
  // Configuration=Debug, so Release/publish output ships production values alone.
  "ReControl": {
    "ApiBaseUrl": "http://localhost:3003",
    "WsUrl": "ws://localhost:3003/cable",
    "LogLevel": "debug"
  }
}
