API_BASE_URL=http://localhost:3000
WS_URL=ws://localhost:3000/cable
ENVIRONMENT=dev
# Minimum log level recorded (Logs tab + file): debug | info | warn | error.
# Unset = debug (record everything). Use warn in production to drop the
# high-frequency per-event Info logging that fires during streaming.
LOG_LEVEL=debug
